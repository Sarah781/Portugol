programa {
  funcao inicio() {
    inteiro a,b,c,d

    leia(a,b,c,d)

    if(b > c) && (d > a) && ((c + d) > (a + b)) && (c / 2) && ( d / 2) && (a / 2) {
      escreva("Valores aceitos")
    }
    else{
      escreva("Valores não aceitos")
    }    
  }
}

//arrumar
